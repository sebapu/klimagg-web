# KlimaGG-Web — requirements.txt
# ---------------------------------------------
# Direct runtime dependencies for the generic web application.
# The public repository uses SQLite and does not run Alembic automatically
# or bundle project-specific data/model pages.
# ---------------------------------------------

fastapi>=0.110,<1.0
uvicorn[standard]>=0.24,<1.0
jinja2>=3.1,<4.0

sqlalchemy>=2.0,<3.0

pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
email-validator>=2.1,<3.0

PyJWT>=2.8,<3.0
